# GitHub Personal Access Token with `repo` scope
GITHUB_TOKEN=

# Groq API key (used for the review-drafting LLM calls)
GROQ_API_KEY=

# OpenAI API key (used only for embeddings in Phase 3 — text-embedding-3-small)
OPENAI_API_KEY=

# Postgres connection string (matches docker-compose.yml defaults)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pr_sentinal
